# ROADMAP.md — مسیر ساخت سایت Artvoid با Claude Code

> ترتیب مراحل مهم است. هر مرحله را کامل کن، بیلد بگیر، بعد برو مرحله بعد.
> در ابتدای هر سشن Claude Code، این فایل و CLAUDE.md و DESIGN.md خوانده می‌شوند.

---

## مرحله ۰ — بوت‌استرپ (Claude Code خودکار انجام می‌دهد — پروتکل در CLAUDE.md)

- [ ] git init + .gitignore + کامیت اسناد
- [ ] نصب اسکیل ui-ux-pro-max
- [ ] نصب Graphify + ساخت .graphifyignore (گراف پروژه برای صرفه‌جویی توکن)
- کاربر فقط باید ریپوی ریموت `artvoid-site` را در گیت‌هاب (simiyari) بسازد و remote را وصل کند

## مرحله ۱ — اسکلت پروژه

- [ ] راه‌اندازی Next.js 16 + TypeScript + Tailwind v4 با App Router (create-next-app سپس ارتقا به Tailwind v4 در صورت نیاز)
- [ ] تنظیم `next.config`: static export، basePath در صورت نیاز GitHub Pages
- [ ] تعریف همه توکن‌های DESIGN.md در بلوک `@theme` فایل globals.css — Tailwind v4، بدون فایل config (شامل dark mode با data-theme)
- [ ] راه‌اندازی RTL: `dir="rtl"` و `lang="fa"` در layout، تست فونت self-hosted
- [ ] GitHub Actions workflow برای دیپلوی (کپی از الگوی IDI Center)
- [ ] فونت self-hosted با next/font/local — هیچ منبع خارجی (قانون ایران در CLAUDE.md)
- [ ] بعد از اولین کد: `graphify .` و `graphify hook install` — از این به بعد گراف با هر کامیت خودکار تازه می‌شود
- [ ] **معیار پایان:** صفحه خالی با فونت فارسی درست، دو حالت روشن/تیره، دیپلوی موفق روی GitHub Pages

## مرحله ۲ — کتابخانه کامپوننت‌های پایه

قبل از هر صفحه‌ای، این‌ها ساخته و در یک صفحه `/dev/ui` (مخفی) نمایش داده شوند:
- [ ] Button (primary / ghost / اندازه‌ها / حالت لودینگ)
- [ ] Card، Input، Textarea، Select، Badge
- [ ] Container، Section (با فاصله‌گذاری استاندارد)
- [ ] Navbar (با منوی موبایل) و Footer
- [ ] کامپوننت Reveal (انیمیشن ورود با Intersection Observer)
- [ ] ThemeToggle (روشن/تیره)
- [ ] **معیار پایان:** صفحه /dev/ui همه اجزا را در هر دو حالت رنگی بدون نقص نشان دهد

## مرحله ۳ — صفحات اصلی (به ترتیب)

1. [ ] **خانه** — Hero (تیتر + جمله موقعیت‌یابی) → خدمات (۳ کارت) → نمونه‌کارهای برجسته → فرآیند کار (۴ قدم) → CTA
2. [ ] **نمونه‌کارها** (`/work`) — گرید پروژه‌ها + صفحه Case Study برای هر پروژه (Ganjriz، IDI Center): مشکل ← راه‌حل ← نتیجه
3. [ ] **خدمات** (`/services`) — یک صفحه با سه بخش: طراحی سایت اختصاصی / نرم‌افزار سفارشی / پشتیبانی و نگهداری. هر بخش: فرآیند + بازه قیمت + FAQ
4. [ ] **درباره** (`/about`) — داستان برند، رویکرد، چرا Artvoid
5. [ ] **شروع پروژه** (`/start`) — فرم چندمرحله‌ای: نوع پروژه ← بودجه ← زمان‌بندی ← اطلاعات تماس
6. [ ] **معیار پایان:** Lighthouse بالای ۹۵ در همه صفحات، تست کامل موبایل

## مرحله ۴ — بخش Showcase (تمایز اصلی!)

- [ ] صفحه `/showcase` — نمایش زنده قابلیت‌های UI/UX (لیست کامل در SHOWCASE.md)
- [ ] اولویت با ۵ دموی اول لیست
- [ ] **معیار پایان:** هر دمو در موبایل هم روان اجرا شود

## مرحله ۵ — سئو و انتشار

- [ ] sitemap.xml، robots.txt، metadata کامل فارسی، og:image برای هر صفحه
- [ ] Schema.org (Organization + Service)
- [ ] اتصال دامنه artvoid.ir به GitHub Pages
- [ ] ثبت در Google Search Console
- [ ] ۳ مقاله اول بلاگ (کلمات کلیدی: «طراحی سایت اختصاصی»، «طراحی سایت جواهرات»، «هزینه طراحی سایت ۱۴۰۵»)

## مرحله ۶ — فازهای بعدی (بعد از انتشار)

- بخش قالب‌های مدیریت‌شده (گالری + دموی زنده + فرم سفارش با آپشن‌ها)
- هسته پنل مدیریت محتوا (تصمیم معماری جدا — Payload CMS یا ساخت اختصاصی)
- صفحه محصول حسابداری طلا (لندینگ + دموی ویدیویی + فرم درخواست دمو)

---

## الگوی شروع هر سشن Claude Code

فقط همین را بنویس — بقیه را خودش از CLAUDE.md و PROGRESS.md می‌فهمد:

```
بخون و ادامه بده.
```

CLAUDE.md به‌طور خودکار خوانده می‌شود و طبق پروتکل، PROGRESS.md و این نقشه را می‌خواند،
وضعیت فعلی را گزارش می‌دهد، برنامه کار بعدی را به فارسی ساده اعلام می‌کند و منتظر تأیید می‌ماند.
